let aka = 'Deadpool';
let realName = 'Wade Wilson';
let completeName = aka + ' es ' + realName;
let templateName = `${aka} es ${realName}`;

console.log(completeName === templateName); // comparasion sintactica de lo que ambas cadenas muestran